<UserControl x:Class="Metrosun.UserControls.DailyItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="80">
    
    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneChromeBrush}">
        <StackPanel Margin="25,10" Orientation="Horizontal">
            <TextBlock x:Name="DateText" Text="1 FRI" FontWeight="Thin" Width="60" TextAlignment="Center" />
            <StackPanel Orientation="Horizontal">
                <Image x:Name="WeatherIcon" Source="..\Images\WICs\sunny.png" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>